# nix
My nix config

## Note to self

Install nix `curl --proto '=https' --tlsv1.2 -sSf -L https://install.determinate.systems/nix | sh -s -- install`
Build config on my mac: `sudo darwin-rebuild switch --flake .#macbook-air-m2`

# to do

- [ ] Setup mac whisper
- [ ] Setup JankyBorders properly https://github.com/FelixKratz/JankyBorders
- [ ] Define chrome (more) extensions (currently broken)
- [ ] Define zed extension spell check config
- [x] Setup a tiling wm, probably aerospace
- [x] Fix case where karabiner.ts fails because the profile names dont exist
- [x] Make the karabiner script cleaner and more composable
- [x] Setup "desktop and dock" settings
- [x] Define keyboard layouts
- [x] Define themes for: ghostty, zed, opencode, chrome, macos(light/dark)
